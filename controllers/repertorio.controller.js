/*import { cancionero } from "../models/repertorio.model.js"

const getcanciones = async (req, res) => {
    try {
        const cancion = await cancionero.findAll()
        return res.json(cancion)
    } catch (error) {
        console.log(error)
        return res.status(500).json({ ok: false })
    }
}

export const cancionera = {
    getcanciones,
}
*/
